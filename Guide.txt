#run all the following code in command prompt

mkdir "Aircraft Project"  
#cd into it
#install git @  https://git-scm.com/downloads
git clone https://github.com/IamJk28/Aircraft
xcopy /E Aircraft ..

#Now we create a new virtual environment for all modules
python -m venv odn
odn\Scripts\activate
python-m pip install --upgrade pip
pip install ipykernel
python -m ipykernel install --user --name=odn
jupyter notebook
#open of Aircraft model.ipynb
#run steps 0-2 of Aircraft model.ipynb
#now run step 2.5 of Aircraft model.ipynb, installing dependencies until it runs properly (for me dependencies are tensorflow cycler kiwisolver)
#run step 3 of Aircraft model.ipynb
#copy images and corresponding labels from MS teams to  Aircraft Project/Tensorflow/workspace/images/collectedimages
#run step 4,5,6 of Aircraft model.ipynb










#possible dependencies (in terms of : pip install matplotlib pip install cycler pip install kiwisolver
pip install contourpy pip install fonttools pip install protobuf pip install matplotlib ==3.2 pip install opencv-python pip install --upgrade pyqt5 lxml  install tensorflow --upgrade
